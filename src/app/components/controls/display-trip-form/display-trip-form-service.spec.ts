import { DisplayTripFormService } from './display-trip-form-service';

describe('DisplayTripFormService', () => {
  it('should create an instance', () => {
    expect(new DisplayTripFormService()).toBeTruthy();
  });
});
